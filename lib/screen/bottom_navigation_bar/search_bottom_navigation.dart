import 'package:flutter/material.dart';
class SearchBottomNavigation extends StatelessWidget {
  const SearchBottomNavigation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
